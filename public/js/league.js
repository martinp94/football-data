$(() => {
	/*$('.league-page-navigation > div').click((e) => {
		const param = e.currentTarget.getAttribute('rel');

		const currentLocation = location.search.substring(0, location.search.indexOf('&')) || location.search;

		location.href = currentLocation + '&' + param;
	});
	*/

	/*$('.league-page-navigation > div').click((e) => {
		const param = e.currentTarget.getAttribute('rel');

		const currentLocation = location.search || location.href;

		console.log(currentLocation);

		location.href = currentLocation + '/' + param;
	});*/
	
});